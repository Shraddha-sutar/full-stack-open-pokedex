name: Hello World
on:
  push:
    branches:
      - main
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: echo "Hello World!"
      - name: Print date
        run: date
      - name: List files
        run: ls -l
